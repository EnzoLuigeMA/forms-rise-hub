# Variáveis de ambiente da função serverless /api/meta-capi (configurar na Vercel).
# Vercel → projeto forms-rise-hub → Settings → Environment Variables.
# NUNCA commite o token real — ele fica só na Vercel.

META_PIXEL_ID=2238173290034845
META_ACCESS_TOKEN=coloque_o_token_da_meta_aqui
